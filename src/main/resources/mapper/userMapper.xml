<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.users.mapper.UserMapper">
	<!-- 목록조회 -->
	<select id="getUserList"
		resultType="com.board.users.domain.UserDTO">
		SELECT *
		FROM TUSER
		ORDER BY
		USERID ASC

	</select>

	<!-- 메뉴 조회 -->
	<select id="getUser" resultType="com.board.users.domain.UserDTO">
		SELECT *
		FROM TUSER
		WHERE USERID = #{ userid }
	</select>

	<!-- 메뉴 추가 -->
	<insert id="insertUser">

		INSERT INTO TUSER
		(
		USERID,
		PASSWD,
		USERNAME,
		EMAIL,
		UPOINT,
		INDATE
		)
		VALUES
		(
		#{ userid },
		#{ passwd },
		#{ username },
		#{ email },
		#{ upoint },
		SYSDATE
		)

	</insert>

	<!-- 목록삭제 -->
	<delete id="deleteUser">

		DELETE FROM TUSER
		WHERE USERID = #{ userid }

	</delete>

	<!-- 메뉴 수정 -->
	<update id="update">
		UPDATE TUSER
		SET USERNAME = #{ username },
		PASSWD = #{ passwd },
		EMAIL = #{email}
		WHERE USERID = #{userid}
	</update>





</mapper>